services:
  - type: web
    name: aiac-cb247fd6-web
    runtime: static
    rootDir: todo-app
    buildCommand: "npm install && npx vite build"
    staticPublishPath: dist
    buildFilter:
      paths:
        - todo-app/**
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
